# Base URL of the Falcon backend API.
# Dev: the backend runs as a separate process on port 8000 (backend PORT).
# Prod: leave unset or set to "" when Falcon serves the built frontend itself.
PUBLIC_API_BASE_URL=http://localhost:8000
